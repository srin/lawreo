<%= form_for [answer, Answercomment.new] do |f| %>
  

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
